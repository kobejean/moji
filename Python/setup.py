from setuptools import setup

setup(name='moji',
      version='0.0',
      description='',
      url='http://github.com/kobejean/moji',
      author='kobejean',
      author_email='kobejean@me.com',
      # license='',
      packages=[
          'moji',
      ],
      install_requires=[
          'regex',
      ],
      zip_safe=False)
